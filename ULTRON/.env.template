# ==============================================================================
# ULTRON Multi-Agent Engine Configuration
# ==============================================================================

# LLM Backend: "local" (Recommended - 100% offline & free) or "api" (Cloud)
LLM_BACKEND=local

# Local LLM Server Configuration (Ollama / LM Studio / LocalAI)
LOCAL_LLM_URL=http://127.0.0.1:11434/v1
LOCAL_MODEL_NAME=llama3.1:8b

# Cloud API Key Fallbacks (Optional - only used if LLM_BACKEND=api)
NVIDIA_API_KEY=
NVIDIA_MODEL_NAME=meta/llama-3.1-8b-instruct
