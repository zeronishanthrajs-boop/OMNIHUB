version: '3.8'

services:
  stock-pulse:
    build: .
    container_name: stock-pulse-engine
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    volumes:
      - ./cache/disk:/app/cache/disk
